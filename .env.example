# Server Configuration
PORT=8888

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=apidb

# HIGH PERFORMANCE Configuration (10k+ TPS)
# Rate limiting praticamente ilimitado
RATE_LIMIT_REQUESTS=999999
RATE_LIMIT_PERIOD=1

# Throttling desabilitado (sem delay artificial)
THROTTLE_MIN_MS=0
THROTTLE_MAX_MS=0

# ⚠️ Para TESTES com limitações, use:
# Rate limiting: 100 requests per second
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_PERIOD=1

# Throttling: 100-500ms delay
# THROTTLE_MIN_MS=100
# THROTTLE_MAX_MS=500
